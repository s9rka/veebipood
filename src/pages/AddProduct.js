import { Link } from "react-router-dom";

function AddProduct() {
    return (
    <div>
        <Link to="/admin">
            <button>Tagasi</button>
        </Link>
    </div>)
}

export default AddProduct;